.search
    width: 100%
    rhythm-borders(2px, 0.5, $base-font-size, solid lighten(#000, 90%))
    border-radius: 5px
    transition: all 1s ease
    trailer(1)

    &:focus
        background: lighten(#000, 80%)
        border-color: lighten(#000, 80%)
        color: #FFF
        outline: none
